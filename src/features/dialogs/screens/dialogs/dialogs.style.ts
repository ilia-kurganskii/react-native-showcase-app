import { StyleSheet } from 'react-native';

export const dialogsStyles = StyleSheet.create({
  container: {
    ...StyleSheet.absoluteFillObject,
    backgroundColor: '#00000050',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
